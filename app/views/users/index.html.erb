<h1 class="page-header text-center"> Users </h1>
<% @users.each do |user| %>
	<div class="row">
		<div class="col-md-8 col-md-offset-2 text-center user_padding">
			<%=link_to image_tag(user.avatar.url(:thumb), class: "img-thumbnail "), user_path(user) %>
		</div>
	</div>
<% end %>